jQuery(document).ready(function($) { const selectRadio = document.getElementById("select_page_mode"); const customRadio = document.getElementById("custom_url_mode"); const dropdown = document.getElementById("page-selector-dropdown"); const customInput = document.getElementById("custom-url-input"); const pageSelector = document.getElementById("page-assets-selector"); const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]')); tooltipTriggerList.forEach(function (tooltipTriggerEl) { new bootstrap.Tooltip(tooltipTriggerEl); }); function toggleInputMode() { if (selectRadio.checked) { dropdown.style.display = "block"; customInput.style.display = "none"; } else { dropdown.style.display = "none"; customInput.style.display = "inline-flex"; pageSelector.selectedIndex = 0; setTimeout(() => { fetchAssetsByUrl(); }, 100); } $('#asset-list-css-container, #additional-css-files-list, #asset-list-js-container, #additional-js-files-list').html(''); $('.all-assets-button-group').addClass('d-none'); } selectRadio.addEventListener("change", toggleInputMode); customRadio.addEventListener("change", toggleInputMode); $('.nav-tab-wrapper a').on('click', function(e) { e.preventDefault(); $('.nav-tab-wrapper a').removeClass('nav-tab-active'); $(this).addClass('nav-tab-active'); $('.optimizer-section').hide(); $('.plugin-optimizer').hide(); $('#'+$(this).attr('data-tab')).show(); if($(this).attr('data-tab') === 'images' || $(this).attr('data-tab') === 'minify'){ $('#page-selector-box').hide(); }else{ $('#page-selector-box').show(); } }); $('#page-assets-selector').on('change', function(event) { event.preventDefault(); const pageId = $(this).val(); if (!pageId) return; $('.asset-list').html('<p>Loading assets...</p>'); $.post(pageAssetsOptimizer.ajaxurl, { action: 'page_assets_get_assets', page_id: pageId, nonce: pageAssetsOptimizer.nonce }, function(response) { if (response.success) { updateAssetList(response.data); $('.all-assets-button-group').removeClass('d-none'); loadAssetsWithPreferences(pageId); } else { $('.asset-list').html('<p class="error">Error loading assets</p>'); } }); }); function loadAssetsWithPreferences(pageId) { Promise.all([ $.get(pageAssetsOptimizer.ajaxurl, { action: 'page_assets_get_preferences', page_id: pageId, nonce: pageAssetsOptimizer.nonce }) ]).then(function(responses) { renderAssetsWithPreferences(responses[0].data); }).catch(function(error) { console.log(error); }); } function renderAssetsWithPreferences(assets) { let assetsCSS = $('#asset-list-css-container ul li'), assetsJS = $('#asset-list-js-container ul li'), assetsPlugin = $('#plugin-list ul li'); let collectAllRegexCSS = assets.styles_to_remove_by_regex, collectAllRegexJS = assets.scripts_to_remove_by_regex; if (assetsCSS.length) { assetsCSS.each(function() { const handleMatch = assets.styles_to_remove?.includes($(this).find('.asset-handle').text()); const regexMatch = assets.styles_to_remove_by_regex?.some(regexString => { try { const cleanedPattern = regexString.replace(/\\\\/g, '\\'); const pattern = new RegExp(cleanedPattern); return pattern.test($(this).find('input').val()); } catch (e) { showToast('Invalid regex: ' + regexString + ' - ' + e, 'error'); return false; } }); for (let i = collectAllRegexCSS.length - 1; i >= 0; i--) { try { const cleanedPattern = collectAllRegexCSS[i].replace(/\\\\/g, '\\'); const regex = new RegExp(cleanedPattern); if (regex.test($(this).find('input').val())) { collectAllRegexCSS.splice(i, 1); } } catch (e) { showToast('Invalid regex in collectAllRegexCSS: ' + collectAllRegexCSS[i] + ' - ' + e, 'error'); } } if(handleMatch === true || regexMatch === true){ $(this).find('input').prop('checked', true); $(this).addClass('selected'); } }); let addintionalCSSHTML = ''; if(collectAllRegexCSS.length){ collectAllRegexCSS.forEach(file => { addintionalCSSHTML += ` <div class="additional-file-item d-flex align-items-center mb-2 justify-content-between"> <input type="hidden" name="additional_css_files[]" value="${file.replace(/\\/g, '')}"> <span class="me-2">${file.replace(/\\/g, '')}</span> <button class="btn btn-sm btn-outline-danger remove-file-btn">Remove</button> </div>`; }); $(`#additional-css-files-list`).html(addintionalCSSHTML); } } if (assetsJS.length) { assetsJS.each(function() { const handleMatch = assets.scripts_to_remove?.includes($(this).find('.asset-handle').text()); const regexMatch = assets.scripts_to_remove_by_regex?.some(regexString => { try { const cleanedPattern = regexString.replace(/\\\\/g, '\\'); const pattern = new RegExp(cleanedPattern); return pattern.test($(this).find('input').val()); } catch (e) { showToast('Invalid regex: ' + regexString + ' - ' + e, 'error'); return false; } }); for (let i = collectAllRegexJS.length - 1; i >= 0; i--) { try { const cleanedPattern = collectAllRegexJS[i].replace(/\\\\/g, '\\'); const regex = new RegExp(cleanedPattern); if (regex.test($(this).find('input').val())) { collectAllRegexJS.splice(i, 1); } } catch (e) { showToast('Invalid regex in collectAllRegexJS: ' + collectAllRegexJS[i] + ' - ' + e, 'error'); } } if(handleMatch === true || regexMatch === true){ $(this).find('input').prop('checked', true); $(this).addClass('selected'); } }); let addintionalJSHTML = ''; if(collectAllRegexJS.length){ collectAllRegexJS.forEach(file => { addintionalJSHTML += ` <div class="additional-file-item d-flex align-items-center mb-2 justify-content-between"> <input type="hidden" name="additional_js_files[]" value="${file.replace(/\\/g, '')}"> <span class="me-2">${file.replace(/\\/g, '')}</span> <button class="btn btn-sm btn-outline-danger remove-file-btn">Remove</button> </div>`; }); $(`#additional-js-files-list`).html(addintionalJSHTML); } } if (assetsPlugin.length) { assetsPlugin.each(function() { const handleMatch = assets.plugins_to_remove?.includes($(this).find('input').val()); if(handleMatch === true){ $(this).find('input').prop('checked', true); $(this).addClass('selected'); } }); } } function updateAssetList(assets) { let htmlCSS = ''; if (assets.css && assets.css.length) { htmlCSS += `<div class="asset-list-header position-sticky top-0 z-index-2 p-3"> <h5>CSS Assets</h5> <input type="text" class="asset-search form-control" placeholder="Search CSS files..." data-asset-type="css"/> </div> <div class="asset-list"> <ul>`; let cssCount = 0; assets.css.forEach(file => { const handleDisplay = file.handle === 'no-handle' ? '<span class="no-handle">(no handle)</span>' : `<span class="asset-handle">${file.handle}</span>`; htmlCSS += `<li class="asset-list-row" data-src="${file.src}" data-handle="${file.handle}"> <input id="css_${cssCount}_assets" type="checkbox" name="css_assets[]" value="${file.src}"> <label for="css_${cssCount}_assets"> <div class="asset-info d-flex flex-column"> ${handleDisplay} <span class="asset-src">${file.src}</span> </div> </label> </li>`; cssCount++; }); htmlCSS += '</ul></div>'; } $('#asset-list-css-container').html(htmlCSS); let htmlJS = ''; if (assets.js && assets.js.length) { htmlJS += `<div class="asset-list-header position-sticky top-0 z-index-2 p-3"> <h5>JS Assets</h5> <input type="text" class="asset-search form-control" placeholder="Search JS files..." data-asset-type="js"/> </div> <div class="asset-list"> <ul>`; let jsCount = 0; assets.js.forEach(file => { const handleDisplay = file.handle === 'no-handle' ? '<span class="no-handle">(no handle)</span>' : `<span class="asset-handle">${file.handle}</span>`; htmlJS += `<li class="asset-list-row" data-src="${file.src}" data-handle="${file.handle}"> <input id="js_${jsCount}_assets" type="checkbox" name="js_assets[]" value="${file.src}"> <label for="js_${jsCount}_assets"> <div class="asset-info d-flex flex-column"> ${handleDisplay} <span class="asset-src">${file.src}</span> </div> </label> </li>`; jsCount++; }); htmlJS += '</ul></div>'; } $('#asset-list-js-container').html(htmlJS); $('.asset-search').on('input', function() { const searchTerm = $(this).val().toLowerCase(); const assetType = $(this).data('asset-type'); $(`#asset-list-${assetType}-container li`).each(function() { let fullSrc = $(this).data('src'); if(fullSrc){ fullSrc = String(fullSrc).toLowerCase(); } const srcParts = fullSrc.split('/'); const filename = srcParts[srcParts.length - 1]; let handle = $(this).data('handle'); if(handle){ handle = String(handle).toLowerCase(); } const matches = filename.includes(searchTerm) || handle.includes(searchTerm); $(this).toggle(matches); }); }); } function fetchAssetsByUrl() { $.ajax({ url: pageAssetsOptimizer.ajaxurl, type: 'POST', data: { action: 'page_assets_optimizer_get_assets', nonce: pageAssetsOptimizer.nonce }, success: function(response) { if (response.success) { $('#asset-list-css-container').html(response.data.css); $('#asset-list-js-container').html(response.data.js); $('.all-assets-button-group').removeClass('d-none'); } else { console.error('Error fetching assets:', response.data); } }, error: function(xhr, status, error) { console.error('AJAX error:', error); } }); } $(document).on('click', '.asset-list-row label', function(event) { event.preventDefault(); if($(this).parent().hasClass('selected')){ $(this).parent().removeClass('selected'); $(this).parent().find('input').prop('checked', false); }else{ $(this).parent().addClass('selected'); $(this).parent().find('input').prop('checked', true); } }); $(document).on('click', '.asset-list-row input[type="checkbox"]', function(event) { if($(this).parent().hasClass('selected')){ $(this).parent().removeClass('selected'); $(this).prop('checked', false); }else{ $(this).parent().addClass('selected'); $(this).prop('checked', true); } }); $(document).on('click', '.select-all', function() { const type = $(this).data('type'); $(`#asset-list-${type}-container input[type="checkbox"]`).prop('checked', true); $(`#asset-list-${type}-container li`).addClass('selected'); }); $(document).on('click', '.deselect-all', function() { const type = $(this).data('type'); $(`#asset-list-${type}-container input[type="checkbox"]`).prop('checked', false); $(`#asset-list-${type}-container li`).removeClass('selected'); }); jQuery(document).on('click', '.select-all[data-type="plugin"], .deselect-all[data-type="plugin"]', function(e) { e.preventDefault(); const isSelect = jQuery(this).hasClass('select-all'); jQuery('#plugin input[name="plugin_to_remove[]"]').prop('checked', isSelect); }); jQuery(document).on('click', '.select-all[data-type="css"], .deselect-all[data-type="css"], .select-all[data-type="js"], .deselect-all[data-type="js"]', function(e) { e.preventDefault(); const type = jQuery(this).data('type'); const isSelect = jQuery(this).hasClass('select-all'); jQuery('#file-assets input[name="' + type + '_assets[]"]').prop('checked', isSelect); }); $(document).off('click', '.save-selection, .save-plugin-selection').on('click', '.save-selection, .save-plugin-selection', function(e) { e.preventDefault(); e.stopImmediatePropagation(); const $btn = $(this); if ($btn.hasClass('processing')) return; $btn.addClass('processing'); const pageId = $('#page-assets-selector').val(); if (!pageId) { showToast('Please select a page first', 'error'); $btn.removeClass('processing'); return; } const scriptsToRemove = []; const stylesToRemove = []; const scriptsToRemoveByRegex = []; const stylesToRemoveByRegex = []; const pluginsToRemove = []; $('#asset-list-css-container input:checked').each(function() { const $item = $(this).closest('label'); const handle = $item.find('.asset-handle').text().trim(); const src = $(this).val(); if (handle && handle !== 'no-handle') { stylesToRemove.push(handle); } else { stylesToRemoveByRegex.push(src.split('/').pop().replace(/[.]/g, '\\$&')); } }); if($('#additional-css-files-list input').length){ $('#additional-css-files-list input').each(function() { const src = $(this).val(); stylesToRemoveByRegex.push(src.split('/').pop().replace(/[.]/g, '\\$&')); }); } $('#asset-list-js-container input:checked').each(function() { const $item = $(this).closest('label'); const handle = $item.find('.asset-handle').text().trim(); const src = $(this).val(); if (handle && handle !== 'no-handle') { scriptsToRemove.push(handle); } else { scriptsToRemoveByRegex.push(src.split('/').pop().replace(/[.]/g, '\\$&')); } }); if($('#additional-js-files-list input').length){ $('#additional-js-files-list input').each(function() { const src = $(this).val(); scriptsToRemoveByRegex.push(src.split('/').pop().replace(/[.]/g, '\\$&')); }); } $('.plugin-optimizer input:checked').each(function() { const src = $(this).val(); if (src !== 'no-handle') { pluginsToRemove.push(src); } }); const saveData = {}; saveData[pageId] = { scripts_to_remove: scriptsToRemove, styles_to_remove: stylesToRemove, scripts_to_remove_by_regex: scriptsToRemoveByRegex, styles_to_remove_by_regex: stylesToRemoveByRegex, plugins_to_remove: pluginsToRemove }; $.ajax({ type: 'POST', url: pageAssetsOptimizer.ajaxurl, data: { action: 'page_assets_save_preferences', optimization_data: JSON.stringify(saveData), nonce: pageAssetsOptimizer.nonce }, complete: function() { $btn.removeClass('processing'); }, success: function(response) { if (response.success) { showToast('Preferences saved successfully!'); } else { const errorMsg = response.data?.message || response.data || 'Unknown error'; showToast('Error saving preferences: ' + errorMsg, 'error'); } }, error: function(jqXHR, textStatus, errorThrown) { let errorMsg = 'Server error: ' + textStatus; try { const responseText = jqXHR.responseText.replace(/^[^\{]*/, ''); const response = JSON.parse(responseText); if (response.data?.message) { errorMsg = response.data.message; } } catch (e) { errorMsg = jqXHR.responseText || errorThrown; } showToast(errorMsg, 'error'); } }); }); if (window.location.search.includes('exported=1')) { const downloadStarted = window.location.search.includes('download=1'); if (downloadStarted) { showToast('Export completed! Your download should start automatically.', 'success'); const cleanUrl = window.location.href.split('?')[0]; window.history.replaceState({}, document.title, cleanUrl); } } $(document).on('click', '.add-file-btn', function () { const input = $(this).siblings('.additional-file-input'); const fileType = $(this).data('type'); let filePath = input.val().trim(); if (!filePath) { showToast('Please enter a file name', 'error'); return; } filePath = filePath.split('?')[0].split('#')[0]; const expectedExt = fileType === 'css' ? '.css' : '.js'; if (!filePath.endsWith(expectedExt)) { showToast(`File must end with ${expectedExt}`, 'error'); return; } const isFullPath = /^(https?:\/\/|\/|\.\/|[\w\-]+\/)/i.test(filePath); if (isFullPath) { showToast('Please enter only the file name, not a path or URL', 'error'); return; } const isDuplicate = $(`#additional-${fileType}-files-list input`) .toArray() .some(el => $(el).val().trim() === filePath); if (isDuplicate) { showToast(`This ${fileType.toUpperCase()} file is already added`, 'error'); return; } $(`#additional-${fileType}-files-list`).append(` <div class="additional-file-item d-flex align-items-center mb-2 justify-content-between"> <input type="hidden" name="additional_${fileType}_files[]" value="${filePath}"> <span class="me-2">${filePath}</span> <button class="btn btn-sm btn-outline-danger remove-file-btn">Remove</button> </div> `); input.val(''); }); $(document).on('click', '.remove-file-btn', function() { $(this).closest('.additional-file-item').remove(); }); if (!document.getElementById('pageAssetsToastContainer')) { const toastContainer = document.createElement('div'); toastContainer.id = 'pageAssetsToastContainer'; toastContainer.className = 'position-fixed top-0 end-0 p-3 mt-4'; toastContainer.style.zIndex = '1100'; document.body.appendChild(toastContainer); } function showToast(message, type = 'success') { const toastContainer = document.getElementById('pageAssetsToastContainer'); if(type === 'error'){ type = 'danger'; } const toastEl = document.createElement('div'); toastEl.className = `toast align-items-center text-white bg-${type} border-0`; toastEl.setAttribute('role', 'alert'); toastEl.setAttribute('aria-live', 'assertive'); toastEl.setAttribute('aria-atomic', 'true'); toastEl.innerHTML = ` <div class="d-flex"> <div class="toast-body"> ${message} </div> <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button> </div> `; toastContainer.appendChild(toastEl); const toast = new bootstrap.Toast(toastEl, { autohide: true, delay: 3000 }); toast.show(); toastEl.addEventListener('hidden.bs.toast', function() { toastEl.remove(); }); } $(document).on('click', '.optimize-images', function() { const isEnabled = $('#image-optimization-toggle').is(':checked'); if(isEnabled){ $.ajax({ url: pageAssetsOptimizer.ajaxurl, type: 'POST', data: { action: 'page_assets_save_image_optimization', enabled: isEnabled ? 1 : 0, nonce: pageAssetsOptimizer.nonce }, success: function(response) { console.log(response); if (!response.success) { showToast('Failed to save image optimization preference', 'error'); } else { showToast('Image optimization enabled successfully!', 'success'); } }, error: function(xhr, status, error) { showToast('AJAX error:', error, 'error'); } }); } else { showToast('Image optimization disabled successfully!', 'success'); } }); $(document).on('click', '.optimize-minify', function() { const isEnabledCss = $('#minify-css-toggle').is(':checked'); const isEnabledJs = $('#minify-js-toggle').is(':checked'); if(isEnabledCss || isEnabledJs){ $.ajax({ url: pageAssetsOptimizer.ajaxurl, type: 'POST', data: { action: 'page_assets_save_minify', enabledCss: isEnabledCss ? 1 : 0, enabledJs: isEnabledJs ? 1 : 0, nonce: pageAssetsOptimizer.nonce }, success: function(response) { if (!response.success) { showToast('Failed to save minification preference', 'error'); } else { showToast('Minification enabled successfully!', 'success'); } }, error: function(xhr, status, error) { showToast('AJAX error:', error, 'error'); } }); } else { showToast('Minification disabled successfully!', 'success'); } }); });